name: flowez
description: "FlowEZ - Smart Queue Management System"
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.0
  cloud_firestore: ^6.0.0
  provider: ^6.1.5
  pretty_qr_code: ^3.5.0
  mobile_scanner: ^7.1.1
  uuid: ^4.5.1
  intl: ^0.20.2
  fl_chart: ^1.0.0   # or the latest compatible version
  share_plus: ^11.1.0
  google_fonts: ^6.1.0
  shared_preferences: ^2.2.0
  gal: ^2.3.0
  path_provider: ^2.1.5
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
